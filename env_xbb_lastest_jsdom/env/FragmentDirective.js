// FragmentDirective对象

bodavm.memory.globalobj['FragmentDirective'] = function FragmentDirective() {
  console.log_copy('FragmentDirective 实例化对象参数为 ->', JSON.stringify_bo(arguments, function (k, v) {
    if (v == window) {
      return 'window';
    } else {
      return v;
    }
  }));
  if (!(this instanceof FragmentDirective)) {
    return bodavm.toolsFunc.throwError("TypeError", "Illegal constructor");
  }
  ;
  this._boisinit = bodavm.config.isinit;
};
bodavm.toolsFunc.safeProto(bodavm.memory.globalobj['FragmentDirective'], "FragmentDirective");